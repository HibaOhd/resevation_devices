apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 8080       # port interne du service Kubernetes
      targetPort: 8080 # port exposé par ton backend dans Docker
