apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: ClusterIP   # change from ClusterIP
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080   # choose a port > 30000
